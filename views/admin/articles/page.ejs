<%- include('../../partials/header.ejs') %>
<%- include('../../partials/homenavbar.ejs') %>

<body>

    <div class="container">
        <hr>

        <hr>
        <% result.articles.rows.forEach(article => { %>
            <div class="card">
                <div class="card-header">
                    <h2><%=article.title%></h2>
                </div>
                <div class="card-body">
                    <a href="/<%=article.slug%>" class="btn btn-success">Leia já</a>
                </div>
            </div>

                <br>
        <% }) %>

        <div class="row">
            <div class="col col-12">
                <% if(result.next) { %>
                    <a style="float: right" href="/articles/page/<%=result.page + 1%>">Próxima página</a>
                    <%}%>
            </div>

            <div class="col col-12">
                <% if(result.page != 1 && result.page != 0) { %>
                    <a style="float: left" href="/articles/page/<%=result.page - 1%>">Página anterior</a>
                    <%}%>
            </div>

            <hr>

    </div>

</body>

<%- include('../../partials/footer.ejs') %>